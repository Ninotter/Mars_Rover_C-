namespace Topology.Planet
{
    public static class PlanetBuilder
    {
        static Planet CreatePlanet(string json)
        {
            // Parse JSON
            //int sizeX = parsedJson.Planet.MaxX;
            //int sizeY = parsedJson.Planet.MaxY;
            //switch type, return planet
            return null;
        }
    }
}
